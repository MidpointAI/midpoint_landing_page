Drop client logo files here, matching the paths in components/v2/customer-stories-data.ts:
a-finer-touch.svg, stonegate.svg, starwood.svg. Monochrome SVGs work best; the card
renders them at 24px tall. Until a file exists the card shows the company name instead.
