Client logos, exported from the "Logos" component in the Midpoint Platform Designs
Figma file. Transparent PNGs; the site draws them as CSS masks in the foreground
colour so one file works in light and dark. Paths and aspect ratios live in
components/v2/customer-stories-data.ts (LOGOS). Replace with SVGs when the
clients can supply them; keep the background transparent.
